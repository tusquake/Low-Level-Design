public class CartDBStorage {
    public void saveToDB(ShoppingCart cart) {
        // Logic to save the cart to a database (mocked for now)
        System.out.println("Saving cart to DB with " + cart.getItems().size() + " items.");
    }
}
